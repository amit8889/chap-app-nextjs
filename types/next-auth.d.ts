// // next-auth.d.ts
// import { DefaultSession } from "next-auth";

// // Extend the Session interface from next-auth to add accessToken
// declare module "next-auth" {
//   interface Session extends DefaultSession {
//     accessToken: string;  // Add the accessToken property
//   }

//   interface JWT {
//     accessToken: string;  // Add the accessToken property to JWT as well
//   }
// }
